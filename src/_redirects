# Cloudflare Pages redirects
# Note: The primary redirect handling is done via functions/trailing-slash.ts
# This file serves as a backup for specific paths
# The Functions middleware automatically handles ALL paths without trailing slashes
