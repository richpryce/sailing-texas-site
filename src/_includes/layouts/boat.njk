---
layout: layouts/base.njk
bodyClass: boat-listing-page
---

<article class="boat-detail">
  <div class="boat-detail-header">
    <div class="boat-detail-header-content">
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <ol>
          <li><a href="/">Home</a></li>
          <li><a href="/boats/">Sailboats for Sale</a></li>
          <li aria-current="page">{{ title }}</li>
        </ol>
      </nav>

      <h1>{{ title }}</h1>

      <div class="boat-meta">
        {% if year %}<span class="boat-year">{{ year }}</span>{% endif %}
        {% if location %}<span class="boat-location">📍 {{ location }}</span>{% endif %}
        {% if status == 'sold' %}
          <span class="boat-status sold">SOLD</span>
        {% endif %}
      </div>

      {% if price %}
      <div class="boat-price">{{ price | formatPrice }}</div>
      {% endif %}
    </div>
  </div>

  <div class="boat-detail-content">
    <div class="boat-detail-main">
      {% if images and images.length > 0 %}
      <div class="boat-gallery">
        <div class="boat-gallery-main">
          <img src="{{ images[0] }}" alt="{{ title }}" id="main-boat-image">
        </div>
        {% if images.length > 1 %}
        <div class="boat-gallery-thumbs">
          {% for image in images %}
          <button class="thumb-btn{% if loop.first %} active{% endif %}" data-image="{{ image }}">
            <img src="{{ image }}" alt="{{ title }} - Image {{ loop.index }}">
          </button>
          {% endfor %}
        </div>
        {% endif %}
      </div>
      {% endif %}

      <div class="boat-description">
        {{ content | safe }}
      </div>

      {% if features and features.length > 0 %}
      <div class="boat-features">
        <h2>Features & Equipment</h2>
        <ul class="feature-list">
          {% for feature in features %}
          <li>{{ feature }}</li>
          {% endfor %}
        </ul>
      </div>
      {% endif %}
    </div>

    <aside class="boat-detail-sidebar">
      <div class="contact-card">
        <h3>Interested in this boat?</h3>
        {% if status != 'sold' %}
        <p>Contact the seller for more information or to schedule a viewing.</p>
        {% if contactEmail %}
        <a href="mailto:{{ contactEmail }}?subject=Inquiry about {{ title }} on Sailing Texas" class="btn btn-primary">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
            <polyline points="22,6 12,13 2,6"/>
          </svg>
          Email Seller
        </a>
        {% endif %}
        {% if contactPhone %}
        <a href="tel:{{ contactPhone }}" class="btn btn-secondary">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
          </svg>
          Call {{ contactPhone }}
        </a>
        {% endif %}
        {% else %}
        <p class="sold-notice">This boat has been sold. Browse our other <a href="/boats/">sailboats for sale</a>.</p>
        {% endif %}
      </div>

      <div class="share-card">
        <h3>Share this listing</h3>
        <div class="share-buttons">
          <a href="https://www.facebook.com/sharer/sharer.php?u={{ site.url }}{{ page.url }}" target="_blank" rel="noopener" class="share-btn facebook" aria-label="Share on Facebook">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/></svg>
          </a>
          <a href="https://twitter.com/intent/tweet?url={{ site.url }}{{ page.url }}&text={{ title | urlencode }}" target="_blank" rel="noopener" class="share-btn twitter" aria-label="Share on Twitter">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M23 3a10.9 10.9 0 0 1-3.14 1.53 4.48 4.48 0 0 0-7.86 3v1A10.66 10.66 0 0 1 3 4s-4 9 5 13a11.64 11.64 0 0 1-7 2c9 5 20 0 20-11.5a4.5 4.5 0 0 0-.08-.83A7.72 7.72 0 0 0 23 3z"/></svg>
          </a>
          <button class="share-btn copy-link" onclick="navigator.clipboard.writeText(window.location.href)" aria-label="Copy link">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>
          </button>
        </div>
      </div>

      <div class="similar-boats">
        <h3>Browse More Sailboats</h3>
        <ul>
          <li><a href="/boats/">All Sailboats for Sale</a></li>
          <li><a href="/boats/catalina/">Catalina Sailboats</a></li>
          <li><a href="/boats/hunter/">Hunter Sailboats</a></li>
          <li><a href="/boats/macgregor/">MacGregor Sailboats</a></li>
        </ul>
      </div>
    </aside>
  </div>
</article>
